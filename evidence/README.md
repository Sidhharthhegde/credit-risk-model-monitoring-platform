# Monitoring Evidence

Full monitoring-run evidence is generated locally and is excluded from Git by default.

The public repository may retain only curated synthetic examples containing no raw applicant data, row-level predictions, sensitive outcomes, fitted model artifacts, local database files, or credentials.

Every finalized local run package must contain a manifest and manifest SHA-256.

